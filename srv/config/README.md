# Service Configuration

This directory contains service configuration and environment guidance for SAP BTP migration.

- Use `default-env.json` for Cloud Foundry bindings.
- Use `.env.example` as a local development starting point.
- Future HANA and XSUAA configuration should be managed here as part of secure deployment.
